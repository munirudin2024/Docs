Urutan eksekusi:
1. schema.sql
2. functions.sql
3. sample_data.sql (jangan jalankan di production kecuali isi sample memang ingin dimasukkan)
4. reports.sql (atau jalankan query‑querynya satu per satu)

Linux/mac:
---------- 
docker exec -i postgres_pgdb psql -U pguser -d pgdb < schema.sql
docker exec -i postgres_pgdb psql -U pguser -d pgdb < functions.sql
docker exec -i postgres_pgdb psql -U pguser -d pgdb < sample_data.sql
